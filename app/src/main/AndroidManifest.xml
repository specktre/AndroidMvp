<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.specktre.androidmvp" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.AndroidMvpApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".splash.SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".userselection.UserSelectionActivity"
            android:label="@string/title_activity_user_selection" >
        </activity>
        <activity
            android:name=".usercoderepos.UserCodeReposActivity"
            android:label="@string/title_activity_user_code_repos" >
        </activity>
    </application>

</manifest>
